# Service_creation.yaml

This YAML file defines a Kubernetes Service of type NodePort named "webapp-service" in the default namespace, exposing port 8080 on node port 30080, targeting Pods with the label "name: simple-webapp".