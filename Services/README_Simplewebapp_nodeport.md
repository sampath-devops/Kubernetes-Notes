# Simplewebapp_nodeport.yaml

This YAML file defines a Kubernetes Deployment named "webapp" with 3 replicas for a simple web app, and a NodePort Service to expose it. It includes rolling updates, probes, and resource limits.