# service for simple web app pod
apiVersion: v1
kind: Service
metadata:
  name: webapp-svc
spec:
  type: NodePort
  selector:
    name: webapp
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30008
