# Greeting_pod.yaml

This YAML file defines a simple Kubernetes Pod named "greeting-pod" running a container from the image "busybox" with the command to echo "Hello, Kubernetes!". It demonstrates the basic structure of a Pod manifest, including apiVersion, kind, metadata, and spec.