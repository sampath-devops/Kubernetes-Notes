# resource-limit.yaml

This YAML file defines a Kubernetes Pod named "httpd-pod" running httpd with resource requests and limits for memory and CPU. Comments note fixes for apiVersion and case sensitivity.