# Volume-share.yaml

This YAML file defines a Kubernetes Pod named "volume-share-datacenter" with two containers sharing an emptyDir volume mounted at /tmp/beta, allowing data sharing between containers.