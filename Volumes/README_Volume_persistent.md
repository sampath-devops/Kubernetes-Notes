# Volume_persistent.yaml

This YAML file defines a PersistentVolume (PV) named "pv-datacenter" with 3Gi storage using hostPath, and a PersistentVolumeClaim (PVC) named "pvc-datacenter" to request storage from the PV.