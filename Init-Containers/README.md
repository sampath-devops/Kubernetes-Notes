# Init Containers

This folder contains Kubernetes manifest files demonstrating Init Containers. Init Containers run before the main containers in a Pod, useful for setup tasks.

## Files

- [Init_containers.yaml](Init_containers.yaml): Pod with Init Container example.