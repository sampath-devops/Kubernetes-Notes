# Init_containers.yaml

This YAML file defines a Kubernetes Deployment with an Init Container that writes a message to a shared volume, followed by a main container that reads from it, demonstrating Init Container usage for setup.