# Deployment.yaml

This YAML file defines a Kubernetes Deployment named "deployment-1" with 2 replicas of a Pod running a BusyBox container. It includes a selector to match Pods with the label "tier: busybox-pod" and a template for creating the Pods.