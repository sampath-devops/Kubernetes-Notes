# redis-deployment.yaml

This YAML file defines a Kubernetes Deployment named "redis-deployment" with 1 replica for deploying Redis database using the redis:alpine image. It includes labels and ports for the container.