# Deployment_full.yaml

This YAML file defines a comprehensive Kubernetes Deployment named "webserver-deployment" with 3 replicas of Pods running Nginx containers. It includes shared volumes using emptyDir, resource requests and limits for CPU and memory, and volume mounts for logging.